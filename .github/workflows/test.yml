# ------------DO-NOT-MODIFY-THIS-FILE------------
# This file was automatically generated by github-actions-workflow-ts.
# Instead, modify workflows/test.wac.ts
# ------------DO-NOT-MODIFY-THIS-FILE------------

name: Tests
'on':
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  Tests:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        node:
          - 16
          - 18
          - 20
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install Node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Install Dependencies
        run: pnpm install --no-frozen-lockfile
      - name: Run Tests
        run: pnpm test
      - name: Update Code Coverage Badge
        if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && matrix.node == 20
        uses: we-cli/coverage-badge-action@48a2699b2e537c7519bdc970fb0ecd75c80a698e
